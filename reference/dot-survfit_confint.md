# Internal: compute survival CI bands (survfit-style)

Internal: compute survival CI bands (survfit-style)

## Usage

``` r
.survfit_confint(
  p,
  se,
  logse = TRUE,
  conf.type,
  conf.int,
  selow,
  ulimit = TRUE
)
```
