% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spbp-model-comparison.R
\name{rank_models}
\alias{rank_models}
\title{Rank fitted spbp models by AIC}
\usage{
rank_models(...)
}
\arguments{
\item{...}{One or more fitted \code{"spbp"} objects (MLE).}
}
\value{
A \code{data.frame} with columns \code{fit}, \code{model},
  \code{degree}, \code{aic}, and \code{npars}.
}
\description{
Convenience wrapper around \code{\link{AIC}} that returns models sorted from
lowest to highest AIC.
}
\examples{
\dontrun{
library(spsurv)
data(veteran, package = "survival")
f1 <- bpph(Surv(time, status) ~ 1, data = veteran, degree = 4)
f2 <- bpph(Surv(time, status) ~ karno, data = veteran, degree = 4)
rank_models(f1, f2)
}
}
